// The chat shell lives in the workspace layout (AgentChat). This route ("/")
// represents a new draft chat; the shell reacts to the pathname.
export default function NewChatPage() {
  return null;
}
