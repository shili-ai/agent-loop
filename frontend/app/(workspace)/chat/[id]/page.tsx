// The chat shell (AgentChat) lives in the workspace layout and reads the
// conversation id from the pathname. This page only declares the route.
export default function ChatPage() {
  return null;
}
